import 'package:flutter/material.dart';

class RowText_Test extends StatelessWidget {
  const RowText_Test({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text(
      "Tests",
      style: TextStyle(fontSize: 25),
    );
  }
}
